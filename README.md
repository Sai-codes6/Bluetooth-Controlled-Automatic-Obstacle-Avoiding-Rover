# Bluetooth-Controlled-Automatic-Obstacle-Avoiding-Rover
A dual-mode robotic rover using an Arduino Nano that switches between manual smartphone steering and autopilot. Over Bluetooth, a custom Android app drives it forward, but if the ultrasonic sensor spots an obstacle within 50cm, the rover overrides the user, automatically backs up, and waits for a safe command.
